import React, { useEffect, useMemo, useState } from 'react';
import { Activity, BarChart3, Bot, Brain, CalendarClock, Eye, EyeOff, Flame, HeartHandshake, Inbox, Instagram, KeyRound, Lightbulb, ListFilter, LogOut, MessageCircle, PauseCircle, PlayCircle, RefreshCw, Save, Search, Send, Settings, ShieldAlert, SlidersHorizontal, Users } from 'lucide-react';
import { supabase, supabaseFunctionsUrl } from './lib/supabaseClient';
import StatusScheduler from './StatusScheduler';
import InstagramScheduler from './InstagramScheduler';

type TabKey = 'dashboard' | 'conversations' | 'status' | 'instagram' | 'ai' | 'health' | 'ideas';
type Conversation = { id: string; remote_jid: string; status: string; ai_enabled: boolean; ai_paused: boolean; pause_reason: string | null; suspected_bot: boolean; service_interest: string | null; last_message_at: string | null; updated_at: string; lead_stage?: 'novo' | 'qualificado' | 'orcamento' | 'fechado' | 'perdido'; lead_value_estimate?: number | null; lead_notes?: string | null; next_follow_up_at?: string | null; };
type Message = { id: string; conversation_id: string; sender_type: 'customer' | 'ai' | 'human' | 'system'; from_me: boolean; message_type: string; content: string | null; media_url: string | null; media_mime_type: string | null; created_at: string; };
type Analytics = { total_contacts: number; total_conversations: number; total_inbound_messages: number; total_ai_messages: number; ai_paused_conversations: number; top_service_interest: string | null; };
type AiSettings = { id: number; enabled: boolean; model: string; business_name: string; system_prompt: string; behavior_rules: string; guided_service_options: string; handoff_keywords: string[]; bot_detection_keywords: string[]; gemini_api_key?: string | null; };

const DEFAULT_PROMPT = `Você é a assistente comercial oficial da Código Base.

Objetivo: atender clientes no WhatsApp, entender a necessidade, classificar o serviço e conduzir para orçamento com Jefferson.

Tom: português do Brasil, profissional, humano, objetivo, cordial e consultivo. Responda curto, com clareza, sem textos longos.

Serviços que pode oferecer:
- Sistemas web, SaaS, sites, PWAs e apps
- Chatbots e agentes de IA para WhatsApp/Instagram
- Automações e integrações com APIs/ERP
- Dashboards, Power BI, SQL e dados
- Marketing digital, posts, carrosséis, stories e Reels
- Hardware, manutenção, redes, firewall pfSense/OPNsense e suporte técnico

Regras obrigatórias:
1. Nunca diga que é humana. Se perguntarem, diga que é assistente virtual da Código Base.
2. Nunca invente preço fechado. Use "a partir de" apenas se o cliente perguntar e conduza para diagnóstico.
3. Faça no máximo 2 perguntas por mensagem.
4. Sempre tente identificar: serviço desejado, urgência, cidade/empresa e melhor horário para retorno.
5. Se cliente pedir humano, diga que vai acionar Jefferson e pare de insistir.
6. Se receber imagem ou áudio, analise o conteúdo e responda com base nele.
7. Não responda grupos, robôs, mensagens automáticas ou outras IAs.
8. Termine com CTA leve: "Posso te ajudar a levantar um orçamento inicial?" ou "Quer que eu encaminhe para Jefferson?".

Quando identificar serviço, use esta lógica:
- WhatsApp, atendimento, respostas automáticas -> IA / Chatbot
- site, sistema, aplicativo, painel -> Sistemas / Apps
- relatório, indicador, dashboard, Power BI -> Dashboards / Dados
- Instagram, posts, reels, marketing -> Marketing / Instagram
- computador, rede, lentidão, suporte -> Hardware / Suporte`;

const MODELS = ['gemini-3-flash', 'gemini-3.5-flash', 'gemini-2.5-flash', 'gemini-1.5-flash'];
const DEFAULT_BEHAVIOR_RULES = 'Tamanho da resposta: curto/médio, estilo WhatsApp. Objetivo: qualificar o cliente antes de vender. Fluxo: uma pergunta por vez, sem textão. Não começar sempre listando tudo que a Código Base faz; comece perguntando o que o cliente precisa e aprofunde somente no serviço que ele demonstrar interesse.';
const DEFAULT_GUIDED_OPTIONS = 'FLUXO GUIADO POR SERVIÇO: quando o cliente demonstrar interesse em um serviço, ofereça opções numeradas curtas e permita resposta livre. IA / Chatbot: 1️⃣ Vendas 2️⃣ Suporte 3️⃣ Agendamentos 4️⃣ Dúvidas frequentes 5️⃣ Quero explicar melhor. Sistemas / Apps: 1️⃣ Site profissional 2️⃣ Sistema com login/painel 3️⃣ App/PWA 4️⃣ Loja virtual 5️⃣ Quero explicar melhor. Dashboards / Dados: 1️⃣ Power BI 2️⃣ Planilhas automáticas 3️⃣ Indicadores financeiros 4️⃣ Relatórios gerenciais 5️⃣ Quero explicar melhor. Marketing / Instagram: 1️⃣ Posts e artes 2️⃣ Stories 3️⃣ Reels/vídeos 4️⃣ Gestão de conteúdo 5️⃣ Quero explicar melhor. Hardware / Suporte: 1️⃣ Computador lento 2️⃣ Formatação/limpeza 3️⃣ Rede/Wi-Fi/firewall 4️⃣ Upgrade SSD/RAM 5️⃣ Quero explicar melhor. Se o cliente responder número, interprete a escolha no contexto do serviço. Se responder 5 ou explicar melhor, peça uma descrição livre. Nunca envie mais de um bloco de opções por mensagem.';

const tabs: { key: TabKey; label: string; icon: any; hint: string }[] = [
  { key: 'dashboard', label: 'Dashboard', icon: BarChart3, hint: 'visão geral' },
  { key: 'conversations', label: 'Conversas', icon: Inbox, hint: 'atendimento' },
  { key: 'status', label: 'Status WhatsApp', icon: CalendarClock, hint: 'posts' },
  { key: 'instagram', label: 'Instagram', icon: Instagram, hint: 'agendamento' },
  { key: 'ai', label: 'IA & Prompt', icon: Brain, hint: 'configuração' },
  { key: 'health', label: 'Logs & Saúde', icon: Activity, hint: 'monitoramento' },
  { key: 'ideas', label: 'Ideias', icon: Lightbulb, hint: 'próximos passos' },
];

const ideas = [
  ['Fila de atendimento humano', 'Centralizar conversas pausadas, bot suspeito e pedidos por humano.'],
  ['Resumo automático da conversa', 'IA resume necessidade, urgência, serviço e próximo passo.'],
  ['Resposta sugerida com IA', 'Botão para gerar resposta humana elegante antes de enviar.'],
  ['CRM simples de leads', 'Novo, qualificado, orçamento, fechado e perdido por conversa.'],
  ['Calendário de Status', 'Visão semanal/mensal dos posts agendados e publicados.'],
  ['Biblioteca de mídias', 'Reaproveitar imagens/vídeos e templates por campanha.'],
  ['Prompt sandbox', 'Testar prompt com mensagens simuladas antes de salvar.'],
  ['Resumo diário', 'Enviar relatório diário de leads, status e falhas.'],
];

const StatCard = ({ icon: Icon, label, value, tone = 'cyan' }: any) => (
  <div className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 shadow-2xl backdrop-blur-xl">
    <div className="flex items-center justify-between gap-4"><div><p className="text-xs uppercase tracking-[0.22em] text-gray-500 font-mono">{label}</p><p className="mt-2 text-3xl font-bold text-white">{value ?? 0}</p></div><div className={`rounded-2xl p-3 ${tone === 'orange' ? 'bg-orange-500/15 text-orange-300' : tone === 'red' ? 'bg-red-500/15 text-red-300' : tone === 'green' ? 'bg-green-500/15 text-green-300' : 'bg-cyan-500/15 text-cyan-300'}`}><Icon size={24} /></div></div>
  </div>
);

function Login() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);
  async function submit(e: React.FormEvent) { e.preventDefault(); setLoading(true); setError(''); const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) setError(error.message); setLoading(false); }
  return <div className="min-h-screen bg-[#05070D] text-white flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-white/10 bg-[#08111F]/80 p-8 shadow-[0_0_60px_rgba(0,217,255,0.12)] backdrop-blur-xl"><img src="/logo.png" className="mx-auto mb-6 h-24 w-24 rounded-full object-cover" /><h1 className="text-center text-3xl font-bold">Admin Código Base</h1><p className="mt-2 text-center text-sm text-gray-400">Entre com usuário Supabase Auth.</p><div className="mt-8 space-y-4"><input className="w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none focus:border-cyan-400" placeholder="E-mail" value={email} onChange={(e) => setEmail(e.target.value)} /><input className="w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none focus:border-cyan-400" placeholder="Senha" type="password" value={password} onChange={(e) => setPassword(e.target.value)} /></div>{error && <p className="mt-4 rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-200">{error}</p>}<button disabled={loading} className="mt-6 w-full rounded-xl bg-cyan-400 px-4 py-3 font-bold text-black transition hover:bg-cyan-300 disabled:opacity-60">{loading ? 'Entrando...' : 'Entrar'}</button></form></div>;
}

export default function AdminApp() {
  const [activeTab, setActiveTab] = useState<TabKey>('dashboard');
  const [session, setSession] = useState<any>(null);
  const [loading, setLoading] = useState(true);
  const [analytics, setAnalytics] = useState<Analytics | null>(null);
  const [analyticsExtra, setAnalyticsExtra] = useState<any>({});
  const [serviceCounts, setServiceCounts] = useState<Record<string, number>>({});
  const [conversations, setConversations] = useState<Conversation[]>([]);
  const [selected, setSelected] = useState<Conversation | null>(null);
  const [messages, setMessages] = useState<Message[]>([]);
  const [reply, setReply] = useState('');
  const [sending, setSending] = useState(false);
  const [settings, setSettings] = useState<AiSettings | null>(null);
  const [savingSettings, setSavingSettings] = useState(false);
  const [showKey, setShowKey] = useState(false);
  const [notice, setNotice] = useState('');
  const [search, setSearch] = useState('');
  const [filter, setFilter] = useState<'all' | 'paused' | 'bot' | 'open' | 'novo' | 'qualificado' | 'orcamento' | 'fechado' | 'perdido'>('all');
  const [aiSummary, setAiSummary] = useState('');
  const [aiWorking, setAiWorking] = useState(false);
  const [leadSaving, setLeadSaving] = useState(false);
  const [statusLogs, setStatusLogs] = useState<any[]>([]);
  const [webhookLogs, setWebhookLogs] = useState<any[]>([]);

  useEffect(() => { supabase.auth.getSession().then(({ data }) => { setSession(data.session); setLoading(false); }); const { data: sub } = supabase.auth.onAuthStateChange((_event, session) => setSession(session)); return () => sub.subscription.unsubscribe(); }, []);

  async function loadAll() {
    if (!session?.access_token) return;
    const [analyticsRes, conversationsRes, settingsRes, statusLogsRes, webhookLogsRes] = await Promise.all([
      fetch(`${supabaseFunctionsUrl}/admin-analytics`, { headers: { Authorization: `Bearer ${session.access_token}` } }).then((r) => r.json()).catch(() => ({})),
      supabase.from('cb_whatsapp_conversations').select('*').order('updated_at', { ascending: false }).limit(100),
      supabase.from('cb_ai_settings').select('*').eq('id', 1).single(),
      supabase.from('cb_whatsapp_status_publish_logs').select('*').order('created_at', { ascending: false }).limit(12),
      supabase.from('cb_webhook_logs').select('*').order('created_at', { ascending: false }).limit(12),
    ]);
    setAnalytics(analyticsRes.analytics || null);
    setAnalyticsExtra(analyticsRes || {});
    setServiceCounts(analyticsRes.serviceCounts || {});
    setConversations(conversationsRes.data || []);
    setStatusLogs(statusLogsRes.data || []);
    setWebhookLogs(webhookLogsRes.data || []);
    setSettings(settingsRes.data ? { ...settingsRes.data, system_prompt: settingsRes.data.system_prompt || DEFAULT_PROMPT, behavior_rules: settingsRes.data.behavior_rules || DEFAULT_BEHAVIOR_RULES, guided_service_options: settingsRes.data.guided_service_options || DEFAULT_GUIDED_OPTIONS } : null);
    if (!selected && conversationsRes.data?.[0]) setSelected(conversationsRes.data[0]);
  }

  async function loadMessages(conversationId: string) { const { data } = await supabase.from('cb_whatsapp_messages').select('*').eq('conversation_id', conversationId).order('created_at', { ascending: true }); setMessages(data || []); }
  useEffect(() => { if (session) loadAll(); }, [session]);
  useEffect(() => { if (selected) loadMessages(selected.id); }, [selected?.id]);

  const topServices = useMemo(() => Object.entries(serviceCounts).sort((a, b) => Number(b[1]) - Number(a[1])).slice(0, 6), [serviceCounts]);
  const messagesByDay = useMemo(() => Array.isArray(analyticsExtra.messagesByDay) ? analyticsExtra.messagesByDay : Object.entries(analyticsExtra.messagesByDay || {}).map(([day, count]) => ({ day, count })), [analyticsExtra.messagesByDay]);
  const humanQueue = useMemo(() => conversations.filter((conv) => conv.ai_paused || conv.suspected_bot), [conversations]);
  const filteredConversations = useMemo(() => conversations.filter((conv) => {
    const q = search.toLowerCase().trim();
    const matchesSearch = !q || conv.remote_jid.toLowerCase().includes(q) || (conv.service_interest || '').toLowerCase().includes(q);
    const matchesFilter = filter === 'all' || (filter === 'paused' && conv.ai_paused) || (filter === 'bot' && conv.suspected_bot) || (filter === 'open' && !conv.ai_paused) || (['novo','qualificado','orcamento','fechado','perdido'] as string[]).includes(filter) && (conv.lead_stage || 'novo') === filter;
    return matchesSearch && matchesFilter;
  }), [conversations, search, filter]);
  const keywordText = (value?: string[]) => (value || []).join('\n');
  const parseKeywords = (text: string) => text.split('\n').map((x) => x.trim()).filter(Boolean);

  async function saveSettings() {
    if (!settings || !session?.user?.id) return;
    setSavingSettings(true); setNotice('');
    const { error } = await supabase.from('cb_ai_settings').update({ enabled: settings.enabled, model: settings.model, business_name: settings.business_name, system_prompt: settings.system_prompt, behavior_rules: settings.behavior_rules || DEFAULT_BEHAVIOR_RULES, guided_service_options: settings.guided_service_options || DEFAULT_GUIDED_OPTIONS, gemini_api_key: settings.gemini_api_key || null, handoff_keywords: settings.handoff_keywords, bot_detection_keywords: settings.bot_detection_keywords, updated_by: session.user.id, updated_at: new Date().toISOString() }).eq('id', 1);
    setNotice(error ? `Erro ao salvar: ${error.message}` : 'Configurações da IA salvas com sucesso. Próximas mensagens já usam novo prompt/modelo/chave.');
    setSavingSettings(false);
  }

  async function toggleAi(conv: Conversation, paused: boolean) { await supabase.from('cb_whatsapp_conversations').update({ ai_paused: paused, pause_reason: paused ? 'human_intervention' : null, updated_at: new Date().toISOString() }).eq('id', conv.id); await loadAll(); setSelected({ ...conv, ai_paused: paused, pause_reason: paused ? 'human_intervention' : null }); }
  async function sendManual() { if (!selected || !reply.trim() || !session?.access_token) return; setSending(true); const text = reply.trim(); setReply(''); await fetch(`${supabaseFunctionsUrl}/send-whatsapp-message`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${session.access_token}` }, body: JSON.stringify({ conversationId: selected.id, remoteJid: selected.remote_jid, text, pauseAi: true }) }); await loadMessages(selected.id); await loadAll(); setSending(false); }
  async function summarizeConversation() { if (!selected || !session?.access_token) return; setAiWorking(true); setAiSummary('Gerando resumo...'); try { const res = await fetch(`${supabaseFunctionsUrl}/summarize-conversation`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${session.access_token}` }, body: JSON.stringify({ conversationId: selected.id }) }); const data = await res.json(); if (!res.ok || !data.ok) throw new Error(data.error || 'Falha ao resumir'); setAiSummary(data.summary); } catch (err: any) { setAiSummary(`Erro: ${err.message || err}`); } finally { setAiWorking(false); } }
  async function suggestReply() { if (!selected || !session?.access_token) return; setAiWorking(true); try { const res = await fetch(`${supabaseFunctionsUrl}/suggest-reply`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${session.access_token}` }, body: JSON.stringify({ conversationId: selected.id }) }); const data = await res.json(); if (!res.ok || !data.ok) throw new Error(data.error || 'Falha ao sugerir'); setReply(data.reply || ''); } catch (err: any) { setAiSummary(`Erro ao sugerir resposta: ${err.message || err}`); } finally { setAiWorking(false); } }
  async function saveLeadPatch(patch: Partial<Conversation>) { if (!selected) return; setLeadSaving(true); const { error } = await supabase.from('cb_whatsapp_conversations').update({ ...patch, updated_at: new Date().toISOString() }).eq('id', selected.id); if (!error) { const updated = { ...selected, ...patch }; setSelected(updated); setConversations((items) => items.map((item) => item.id === selected.id ? updated : item)); } setLeadSaving(false); }
  function exportLeadsCsv() { const safe = (value: any) => { const text = String(value ?? '').replace(/"/g, '""'); return /^[=+\-@]/.test(text) ? `"'${text}"` : `"${text}"`; }; const rows = filteredConversations.map((c) => [c.remote_jid, c.service_interest, c.lead_stage || 'novo', c.lead_value_estimate || '', c.next_follow_up_at || '', c.ai_paused ? 'sim' : 'não', c.suspected_bot ? 'sim' : 'não'].map(safe).join(',')); const csv = ['Telefone,Serviço,Etapa,Valor estimado,Follow-up,IA pausada,Bot suspeito', ...rows].join('\n'); const blob = new Blob([csv], { type: 'text/csv;charset=utf-8' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'leads-codigo-base.csv'; a.click(); URL.revokeObjectURL(url); }

  const Dashboard = () => <div className="space-y-6">
    <section className="grid gap-4 md:grid-cols-2 xl:grid-cols-5"><StatCard icon={Users} label="Pessoas" value={analytics?.total_contacts} /><StatCard icon={MessageCircle} label="Conversas" value={analytics?.total_conversations} /><StatCard icon={BarChart3} label="Msgs recebidas" value={analytics?.total_inbound_messages} tone="orange" /><StatCard icon={Bot} label="Msgs IA" value={analytics?.total_ai_messages} /><StatCard icon={PauseCircle} label="IA pausada" value={analytics?.ai_paused_conversations} tone="red" /></section>
    <section className="grid gap-6 lg:grid-cols-3">
      <div className="rounded-3xl border border-white/10 bg-white/[0.03] p-6 lg:col-span-2"><h2 className="mb-4 flex items-center gap-2 font-bold"><Flame className="text-orange-300" /> Serviços mais procurados</h2><div className="grid gap-3 md:grid-cols-2">{topServices.length ? topServices.map(([service, count]) => <div key={service} className="rounded-2xl border border-white/10 bg-black/20 p-4"><p className="text-sm text-gray-300">{service}</p><p className="mt-1 text-2xl font-bold text-cyan-300">{count}</p></div>) : <p className="text-gray-500">Sem dados ainda.</p>}</div></div>
      <div className="rounded-3xl border border-orange-400/20 bg-orange-400/[0.04] p-6"><h2 className="mb-4 flex items-center gap-2 font-bold"><HeartHandshake className="text-orange-300" /> Fila humana</h2><p className="text-4xl font-black text-orange-200">{humanQueue.length}</p><p className="mt-2 text-sm text-gray-400">Conversas pausadas ou bot suspeito.</p><button onClick={() => setActiveTab('conversations')} className="mt-4 rounded-xl bg-orange-400 px-4 py-2 font-bold text-black">Abrir fila</button></div>
    </section>
    <section className="grid gap-6 lg:grid-cols-2"><div className="rounded-3xl border border-cyan-400/20 bg-cyan-400/[0.03] p-6"><h2 className="mb-4 font-bold">Mensagens por dia</h2><div className="space-y-2">{messagesByDay.slice(-7).map((item: any) => <div key={item.day} className="flex items-center gap-3"><span className="w-24 text-xs text-gray-500">{item.day}</span><div className="h-3 flex-1 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-cyan-400" style={{ width: `${Math.min(100, Number(item.count || 0) * 8)}%` }} /></div><span className="w-8 text-right text-sm text-cyan-200">{item.count}</span></div>) || <p className="text-gray-500">Sem série ainda.</p>}</div></div><div className="rounded-3xl border border-white/10 bg-white/[0.03] p-6"><h2 className="mb-4 font-bold">Próximas ações sugeridas</h2><div className="space-y-3">{ideas.slice(0, 4).map(([title, text]) => <div key={title} className="rounded-2xl border border-white/10 bg-black/20 p-4"><p className="font-bold text-white">{title}</p><p className="mt-1 text-sm text-gray-400">{text}</p></div>)}</div></div></section>
  </div>;

  const Conversations = () => <section className="grid gap-6 lg:grid-cols-[380px_1fr]"><div className="rounded-3xl border border-white/10 bg-white/[0.03] p-4"><div className="mb-4 flex items-center justify-between"><h2 className="font-bold">Conversas</h2><button onClick={exportLeadsCsv} className="rounded-lg border border-white/10 px-2 py-1 text-xs text-gray-300">Exportar CSV</button><span className="text-xs text-gray-500">{filteredConversations.length}</span></div><div className="mb-3 flex gap-2"><div className="flex flex-1 items-center gap-2 rounded-xl border border-white/10 bg-black/20 px-3"><Search size={15} className="text-gray-500" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Buscar telefone/serviço" className="min-w-0 flex-1 bg-transparent py-2 text-sm outline-none" /></div><select value={filter} onChange={(e) => setFilter(e.target.value as any)} className="rounded-xl border border-white/10 bg-[#05070D] px-2 text-sm"><option value="all">Todas</option><option value="paused">Pausadas</option><option value="bot">Bot suspeito</option><option value="open">IA ativa</option><option value="novo">Novo</option><option value="qualificado">Qualificado</option><option value="orcamento">Orçamento</option><option value="fechado">Fechado</option><option value="perdido">Perdido</option></select></div><div className="max-h-[680px] space-y-2 overflow-auto pr-1">{filteredConversations.map((conv) => <button key={conv.id} onClick={() => setSelected(conv)} className={`w-full rounded-2xl border p-4 text-left transition ${selected?.id === conv.id ? 'border-cyan-400 bg-cyan-400/10' : 'border-white/10 bg-black/20 hover:bg-white/5'}`}><div className="flex items-center justify-between gap-2"><p className="font-mono text-sm text-white">{conv.remote_jid.replace('@s.whatsapp.net', '')}</p>{conv.ai_paused ? <PauseCircle className="text-orange-300" size={18} /> : <Bot className="text-cyan-300" size={18} />}</div><p className="mt-2 text-xs text-gray-400">{conv.service_interest || 'Serviço ainda não identificado'}</p>{conv.suspected_bot && <p className="mt-2 text-xs text-red-300"><ShieldAlert size={13} className="inline mr-1" />Possível IA/bot</p>}</button>)}</div></div><div className="rounded-3xl border border-white/10 bg-white/[0.03] p-4">{selected ? <><div className="mb-4 flex flex-wrap items-center justify-between gap-3 border-b border-white/10 pb-4"><div><h2 className="font-mono text-lg font-bold">{selected.remote_jid}</h2><p className="text-sm text-gray-400">Interesse: {selected.service_interest || 'não classificado'} • Status IA: {selected.ai_paused ? 'pausada' : 'ativa'}</p></div><button onClick={() => toggleAi(selected, !selected.ai_paused)} className={`rounded-xl px-4 py-2 text-sm font-bold ${selected.ai_paused ? 'bg-cyan-400 text-black' : 'bg-orange-400 text-black'}`}>{selected.ai_paused ? <PlayCircle size={16} className="inline mr-2" /> : <PauseCircle size={16} className="inline mr-2" />}{selected.ai_paused ? 'Reativar IA' : 'Assumir humano'}</button></div><div className="mb-4 grid gap-3 rounded-2xl border border-white/10 bg-black/20 p-4 md:grid-cols-4"><label className="space-y-1"><span className="text-xs text-gray-500">Etapa CRM</span><select value={selected.lead_stage || 'novo'} onChange={(e) => saveLeadPatch({ lead_stage: e.target.value as any })} className="w-full rounded-xl border border-white/10 bg-[#05070D] px-3 py-2 text-sm"><option value="novo">Novo</option><option value="qualificado">Qualificado</option><option value="orcamento">Orçamento</option><option value="fechado">Fechado</option><option value="perdido">Perdido</option></select></label><label className="space-y-1"><span className="text-xs text-gray-500">Valor estimado</span><input value={selected.lead_value_estimate || ''} onChange={(e) => saveLeadPatch({ lead_value_estimate: e.target.value ? Number(e.target.value) : null })} className="w-full rounded-xl border border-white/10 bg-[#05070D] px-3 py-2 text-sm" placeholder="R$" /></label><label className="space-y-1"><span className="text-xs text-gray-500">Follow-up</span><input type="datetime-local" value={selected.next_follow_up_at ? new Date(new Date(selected.next_follow_up_at).getTime() - new Date().getTimezoneOffset() * 60000).toISOString().slice(0,16) : ''} onChange={(e) => saveLeadPatch({ next_follow_up_at: e.target.value ? new Date(e.target.value).toISOString() : null })} className="w-full rounded-xl border border-white/10 bg-[#05070D] px-3 py-2 text-sm" /></label><div className="flex items-end gap-2"><button disabled={aiWorking} onClick={summarizeConversation} className="rounded-xl border border-purple-400/30 bg-purple-400/10 px-3 py-2 text-xs font-bold text-purple-100 disabled:opacity-50">Resumir IA</button><button disabled={aiWorking} onClick={suggestReply} className="rounded-xl border border-cyan-400/30 bg-cyan-400/10 px-3 py-2 text-xs font-bold text-cyan-100 disabled:opacity-50">Sugerir resposta</button></div><label className="space-y-1 md:col-span-4"><span className="text-xs text-gray-500">Notas internas</span><textarea value={selected.lead_notes || ''} onChange={(e) => saveLeadPatch({ lead_notes: e.target.value })} className="min-h-[70px] w-full rounded-xl border border-white/10 bg-[#05070D] p-3 text-sm" placeholder="Notas que não vão para o WhatsApp..." /></label>{aiSummary && <div className="md:col-span-4 rounded-xl border border-purple-400/20 bg-purple-400/10 p-3 text-sm whitespace-pre-wrap text-purple-50">{aiSummary}</div>}{leadSaving && <p className="text-xs text-gray-500">Salvando lead...</p>}</div><div className="h-[520px] space-y-3 overflow-auto rounded-2xl bg-black/25 p-4">{messages.map((msg) => <div key={msg.id} className={`flex ${msg.from_me ? 'justify-end' : 'justify-start'}`}><div className={`max-w-[78%] rounded-2xl px-4 py-3 ${msg.from_me ? msg.sender_type === 'ai' ? 'bg-cyan-500/20 border border-cyan-400/20' : 'bg-orange-500/20 border border-orange-400/20' : 'bg-white/10 border border-white/10'}`}><p className="mb-1 text-[10px] uppercase tracking-wider text-gray-400">{msg.sender_type} • {msg.message_type}</p><p className="whitespace-pre-wrap text-sm text-gray-100">{msg.content || '[mídia recebida]'}</p>{msg.media_url && <a className="mt-2 block text-xs text-cyan-300 underline" href={msg.media_url} target="_blank">abrir mídia</a>}</div></div>)}</div><div className="mt-4 flex gap-3"><input value={reply} onChange={(e) => setReply(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && sendManual()} className="flex-1 rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none focus:border-cyan-400" placeholder="Responder como humano (pausa IA automaticamente)..." /><button disabled={sending || !reply.trim()} onClick={sendManual} className="rounded-xl bg-cyan-400 px-5 py-3 font-bold text-black disabled:opacity-50"><Send size={18} /></button></div></> : <div className="grid h-[620px] place-items-center text-gray-500">Nenhuma conversa selecionada.</div>}</div></section>;

  const AiSettings = () => <section className="rounded-3xl border border-cyan-400/20 bg-cyan-400/[0.03] p-6 shadow-[0_0_50px_rgba(0,217,255,0.08)]"><div className="mb-6 flex flex-wrap items-center justify-between gap-3"><div><h2 className="flex items-center gap-2 text-2xl font-bold"><SlidersHorizontal className="text-cyan-300" /> IA & Prompt</h2><p className="mt-1 text-sm text-gray-400">Prompt, modelo, chave Gemini, palavras de handoff e anti-bot.</p></div><button onClick={saveSettings} disabled={savingSettings || !settings} className="rounded-xl bg-cyan-400 px-5 py-3 font-bold text-black disabled:opacity-50"><Save size={18} className="inline mr-2" />{savingSettings ? 'Salvando...' : 'Salvar IA'}</button></div>{settings && <div className="grid gap-5"><div className="grid gap-4 md:grid-cols-3"><label className="rounded-2xl border border-white/10 bg-black/20 p-4"><span className="mb-2 flex items-center gap-2 text-sm font-bold text-gray-200"><Brain size={16} /> Modelo Gemini</span><select value={settings.model} onChange={(e) => setSettings({ ...settings, model: e.target.value })} className="w-full rounded-xl border border-white/10 bg-[#05070D] px-3 py-3 text-white outline-none focus:border-cyan-400">{MODELS.map((m) => <option key={m}>{m}</option>)}</select></label><label className="rounded-2xl border border-white/10 bg-black/20 p-4"><span className="mb-2 block text-sm font-bold text-gray-200">Nome da empresa</span><input value={settings.business_name} onChange={(e) => setSettings({ ...settings, business_name: e.target.value })} className="w-full rounded-xl border border-white/10 bg-[#05070D] px-3 py-3 outline-none focus:border-cyan-400" /></label><label className="rounded-2xl border border-white/10 bg-black/20 p-4"><span className="mb-2 flex items-center gap-2 text-sm font-bold text-gray-200"><KeyRound size={16} /> Gemini API Key</span><div className="flex gap-2"><input type={showKey ? 'text' : 'password'} value={settings.gemini_api_key || ''} onChange={(e) => setSettings({ ...settings, gemini_api_key: e.target.value })} placeholder="AIza..." className="min-w-0 flex-1 rounded-xl border border-white/10 bg-[#05070D] px-3 py-3 outline-none focus:border-cyan-400" /><button type="button" onClick={() => setShowKey(!showKey)} className="rounded-xl border border-white/10 px-3">{showKey ? <EyeOff size={18} /> : <Eye size={18} />}</button></div></label></div><label><span className="mb-2 block text-sm font-bold text-gray-200">Comportamento da IA</span><textarea value={settings.behavior_rules || DEFAULT_BEHAVIOR_RULES} onChange={(e) => setSettings({ ...settings, behavior_rules: e.target.value })} className="min-h-[130px] w-full rounded-2xl border border-cyan-400/20 bg-cyan-400/5 p-4 font-mono text-sm leading-relaxed outline-none focus:border-cyan-400" /></label><label><span className="mb-2 block text-sm font-bold text-gray-200">Opções numeradas por serviço</span><textarea value={settings.guided_service_options || DEFAULT_GUIDED_OPTIONS} onChange={(e) => setSettings({ ...settings, guided_service_options: e.target.value })} className="min-h-[190px] w-full rounded-2xl border border-orange-400/20 bg-orange-400/5 p-4 font-mono text-sm leading-relaxed outline-none focus:border-orange-400" /></label><label><span className="mb-2 block text-sm font-bold text-gray-200">Prompt principal da IA</span><textarea value={settings.system_prompt} onChange={(e) => setSettings({ ...settings, system_prompt: e.target.value })} className="min-h-[360px] w-full rounded-2xl border border-white/10 bg-black/30 p-4 font-mono text-sm leading-relaxed outline-none focus:border-cyan-400" /></label><div className="grid gap-4 md:grid-cols-2"><label><span className="mb-2 block text-sm font-bold text-gray-200">Palavras que pausam IA e chamam humano</span><textarea value={keywordText(settings.handoff_keywords)} onChange={(e) => setSettings({ ...settings, handoff_keywords: parseKeywords(e.target.value) })} className="min-h-[160px] w-full rounded-2xl border border-white/10 bg-black/30 p-4 font-mono text-sm outline-none focus:border-cyan-400" /></label><label><span className="mb-2 block text-sm font-bold text-gray-200">Palavras para detectar bot/IA</span><textarea value={keywordText(settings.bot_detection_keywords)} onChange={(e) => setSettings({ ...settings, bot_detection_keywords: parseKeywords(e.target.value) })} className="min-h-[160px] w-full rounded-2xl border border-white/10 bg-black/30 p-4 font-mono text-sm outline-none focus:border-cyan-400" /></label></div><label className="flex items-center gap-3 rounded-2xl border border-white/10 bg-black/20 p-4"><input type="checkbox" checked={settings.enabled} onChange={(e) => setSettings({ ...settings, enabled: e.target.checked })} className="h-5 w-5" /><span>IA ativa para novas mensagens elegíveis</span></label>{notice && <p className="rounded-xl border border-cyan-400/20 bg-cyan-400/10 p-3 text-sm text-cyan-100">{notice}</p>}</div>}</section>;

  const Health = () => <section className="grid gap-6 lg:grid-cols-2"><div className="rounded-3xl border border-green-400/20 bg-green-400/[0.03] p-6"><h2 className="mb-4 flex items-center gap-2 text-xl font-bold"><Activity className="text-green-300" /> Saúde</h2><div className="grid gap-3"><StatCard icon={Activity} label="Supabase" value="OK" tone="green" /><StatCard icon={Bot} label="IA" value={settings?.enabled ? 'Ativa' : 'Off'} tone={settings?.enabled ? 'green' : 'red'} /><StatCard icon={CalendarClock} label="Status logs" value={statusLogs.length} /></div></div><div className="rounded-3xl border border-white/10 bg-white/[0.03] p-6"><h2 className="mb-4 font-bold">Últimos logs de Status</h2><div className="max-h-[420px] space-y-2 overflow-auto">{statusLogs.map((log) => <div key={log.id} className="rounded-2xl border border-white/10 bg-black/20 p-3"><p className="text-sm font-bold text-white">{log.event_type} <span className="text-xs text-gray-500">{log.status}</span></p><p className="mt-1 text-xs text-gray-400">{log.message || 'sem mensagem'}</p></div>)}</div></div><div className="rounded-3xl border border-white/10 bg-white/[0.03] p-6 lg:col-span-2"><h2 className="mb-4 font-bold">Últimos webhooks</h2><div className="grid gap-2 md:grid-cols-2">{webhookLogs.map((log) => <div key={log.id} className="rounded-2xl border border-white/10 bg-black/20 p-3"><p className="text-xs text-gray-500">{new Date(log.created_at).toLocaleString('pt-BR')}</p><p className="text-sm text-gray-300">{log.ignored ? `Ignorado: ${log.ignore_reason}` : 'Processado'}</p></div>)}</div></div></section>;

  const Ideas = () => <section className="rounded-3xl border border-purple-400/20 bg-purple-400/[0.03] p-6"><h2 className="mb-2 flex items-center gap-2 text-2xl font-bold"><Lightbulb className="text-purple-300" /> Ideias para evoluir</h2><p className="mb-6 text-sm text-gray-400">Backlog prático para transformar o admin em central comercial.</p><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{ideas.map(([title, text], i) => <div key={title} className="rounded-2xl border border-white/10 bg-black/20 p-5"><span className="rounded-full bg-purple-400/20 px-2 py-1 text-xs text-purple-100">#{i + 1}</span><h3 className="mt-4 font-bold text-white">{title}</h3><p className="mt-2 text-sm text-gray-400">{text}</p></div>)}</div></section>;

  if (loading) return <div className="min-h-screen bg-[#05070D] text-white grid place-items-center">Carregando admin...</div>;
  if (!session) return <Login />;

  return <div className="min-h-screen bg-[#05070D] text-white"><header className="sticky top-0 z-40 border-b border-white/10 bg-[#05070D]/90 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl flex-wrap items-center justify-between gap-4 px-6 py-4"><div className="flex items-center gap-3"><img src="/logo.png" className="h-12 w-12 rounded-full object-cover" /><div><h1 className="text-xl font-bold">Admin Código Base</h1><p className="text-xs text-gray-400">WhatsApp • Instagram • Gemini IA • Status • Analytics</p></div></div><div className="flex items-center gap-3"><button onClick={loadAll} className="rounded-xl border border-white/10 bg-white/5 px-4 py-2 text-sm hover:bg-white/10"><RefreshCw size={16} className="inline mr-2" />Atualizar</button><button onClick={() => supabase.auth.signOut()} className="rounded-xl border border-red-500/30 bg-red-500/10 px-4 py-2 text-sm text-red-100 hover:bg-red-500/20"><LogOut size={16} className="inline mr-2" />Sair</button></div></div><nav className="mx-auto flex max-w-7xl gap-2 overflow-x-auto px-6 pb-4">{tabs.map(({ key, label, icon: Icon, hint }) => <button key={key} onClick={() => setActiveTab(key)} className={`flex min-w-fit items-center gap-2 rounded-2xl border px-4 py-3 text-left transition ${activeTab === key ? 'border-cyan-400 bg-cyan-400/15 text-cyan-100' : 'border-white/10 bg-white/[0.03] text-gray-300 hover:bg-white/10'}`}><Icon size={18} /><span><span className="block text-sm font-bold">{label}</span><span className="block text-[10px] uppercase tracking-wider text-gray-500">{hint}</span></span></button>)}</nav></header><main className="mx-auto max-w-7xl space-y-8 px-6 py-8">{activeTab === 'dashboard' && <Dashboard />}{activeTab === 'conversations' && <Conversations />}{activeTab === 'status' && <StatusScheduler session={session} />}{activeTab === 'instagram' && <InstagramScheduler session={session} />}{activeTab === 'ai' && <AiSettings />}{activeTab === 'health' && <Health />}{activeTab === 'ideas' && <Ideas />}</main></div>;
}
